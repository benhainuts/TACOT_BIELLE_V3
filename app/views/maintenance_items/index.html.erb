hello from maintenance_items index

<p><%=link_to "nouveau" , new_car_maintenance_item_path%></p>

<%if @car.maintenance_items.empty?%>
  <div>
    <p>pas d'opération déclarée dans le plan d'entretien</p>
    <%= link_to "créer le plan d'entretien", car_maintenance_items_call_maintenance_path(@car) %>
  </div>

<%else%>
  <%= link_to "mettre à jour le plan d'entretien", car_maintenance_items_call_maintenance_path(@car) %>
  <ul>
    <%i = 0%>
    <%@car.maintenance_items.each do |item|%>
      <li><%=i%> - <%=link_to item.item_name , car_maintenance_item_path(@car, item)%></li>
      <% i = i+1%>
    <%end%>
  </ul>
<%end%>
